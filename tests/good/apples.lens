var totalApples = 0;

match{
    "(?<numApples>\d+) apples";
    "^(?<name>[^ ]*)"
}


stream{
    print(entry.name);
}

stream{
    totalApples += parseInt(entry.numApples);
    if(totalApples > 10) 
    	print("We picked too many apples: " + totalApples);
}

